Para guardar en github:
------------------------------------------------------------------
git init

si(va a colocar todo)? "git add ." : "git add nombre-del-archivo.extensión"

git commit -m 'aqui va un comentario'

git remote add origin git@github.com:jfbenavid/Domotica_Django.git

si sale(fatal: remote origin already exists)? "git remote rm origin" : no haga esto

git push -u origin master
-------------------------------------------------------------------
Proyecto Domotica
cd Escritorio/Proyectos/Domotica
git init
git add .
git commit -m 'se hicieron cambios a la logica difusa debido a que hacia falta tratarlos como si estuviesen en una clase; se termino el control remoto manual y el control automatico del aire acondicionado; tambien se cambio el proceso del dimmer(se elimino lo que iba dentro del while debido a que no se necesita). Por ultimo, se arregla el touch para el dimmer ya que antes no funcionaba.'
git remote add origin git@github.com:jfbenavid/Domotica_Django_v2.git
git push -u origin master