Para guardar en github:
------------------------------------------------------------------
git init

si(va a colocar todo)? "git add ." : "git add nombre-del-archivo.extensión"

git commit -m 'aqui va un comentario'

git remote add origin git@github.com:jfbenavid/Domotica_Django.git

si sale(fatal: remote origin already exists)? "git remote rm origin" : no haga esto

git push -u origin master
-------------------------------------------------------------------
Proyecto Domotica
cd Escritorio/Proyectos/Domotica
git init
git add .
git commit -m 'se crearon 3 url para las opciones y creacion de usuarios y puertos; el template opciones fue cambiado para que quedara como plantilla para las otras dos, el template de formularios de creacion en las opciones es el mismo y se creo un css nuevo para ocultar el menu de opciones en vista movil; falta agregar el css de los formularios en vista de escritorio'
git remote add origin git@github.com:jfbenavid/Domotica_Django_v2.git
git push -u origin master